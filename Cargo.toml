[package]
name = "voronoi"
version = "0.0.1"
authors = ["André-Patrick Bubel <code@andre-bubel.de>"]
build = "build.rs"

[dependencies]
nalgebra = "0.2"
libc = "*"

[dev-dependencies]
quickcheck = "0.2"

[build-dependencies]
gcc = "0.3"
